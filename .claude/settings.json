{
  "permissions": {
    "allow": [
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000/)",
      "Bash(netstat -ano)",
      "Skill(run)",
      "Skill(run:*)",
      "Bash(which chromium-cli *)",
      "Read(//c//**)"
    ]
  }
}
